import { Message } from "whatsapp-web.js";

export async function addClientAdmin(message: Message) {
  // let contacts = process.env
  //This takes a lot of work and prob not that useful too :: I will do it when I'm in the mood
}
